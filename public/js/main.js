document.addEventListener('DOMContentLoaded', function() {
	let pageIndex = [...document.getElementsByClassName('page-link')];
	let pageTarget = document.getElementsByClassName('active-page')[0];
	// var tin = 8;
	// var page = 13;
	// var i = 0;

	// for(let i = 0; i < pageIndex.length; i++) {
	// 	pageIndex[i].addEventListener("click", function(event) {
	// 		this.setAttribute('href', `products?page=${i}`);
	// 		this.classList.add('active-page');
	// 	})
	// }
	// while(i < page) {
		
	// }
})
